#include <bits/stdc++.h>
using namespace std;

#define MAXN 100
#define MAXM 100
#define add(a,b,c) to[z] = b, cap[z] = c, from[z] = a, ant[z] = adj[a], adj[a] = z++

int to[MAXM], cap[MAXM], from[MAXM], ant[MAXM], adj[MAXN];

void bfs() {
	
	
}

int main() {
	printf("Noob");
}